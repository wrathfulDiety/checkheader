{
  "name": "checkheader-report-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "checkHEADer Report Mode — paste HTTP response headers, get a report-ready security observation. 100% client-side.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "vite build --mode single",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
